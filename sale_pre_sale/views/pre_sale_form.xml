<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_order_form_inherit_sale" model="ir.ui.view">
        <field name="name">sale.view.form.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="iting_customization.sale_order_form_view_iting"/>
        <field name="arch" type="xml">
            <field name="salesman_id" position ="after">
                <field name="pre_sale" widget="many2many_tags" attrs="{'readonly':[['state','not in', ['draft']]]}"/>
            </field>
        </field>
    </record>

</odoo>
